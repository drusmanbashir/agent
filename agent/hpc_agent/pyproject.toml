[project]
name = "hpc-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
hpc-agent = "hpc_agent.cli:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["hpc_agent"]
